{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Requests Integration",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/intg-requests/driver.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}